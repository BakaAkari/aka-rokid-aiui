{
  "name": "aka-rokid-aiui",
  "version": "0.1.0",
  "private": true,
  "description": "Long-term Rokid AIUI agents monorepo: glasses diagnostics, camera experiments, and future Hermes integration.",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "test:agents": "node --test agents/connection-diagnostics/test/ agents/combat-power-detector/test/",
    "validate": "node scripts/validate.mjs",
    "check:secrets": "node scripts/check-secrets.mjs",
    "check:versions": "node scripts/check-versions.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}